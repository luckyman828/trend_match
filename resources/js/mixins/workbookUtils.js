import {
    parseWorkbookToRowsAndCells,
    validateMappedField,
    autoMapField,
    instantiateProductsFromMappedFields,
} from '../helpers/workbookUtils'

export default {
    methods: {
        parseWorkbookToRowsAndCells,
        validateMappedField,
        autoMapField,
        instantiateProductsFromMappedFields,
    },
}
